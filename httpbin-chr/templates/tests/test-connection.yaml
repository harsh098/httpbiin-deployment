apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "httpbin-chr.fullname" . }}-test-connection"
  labels:
    {{- include "httpbin-chr.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "httpbin-chr.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
